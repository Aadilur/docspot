# Railway / Nixpacks config for the backend service
# Use this when the Railway service Root Directory is set to `backend/`.

[phases.install]
cmds = ["npm install --include=dev --no-fund --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
